步骤 1：数据策略修正 — 移 27 张 FN 进 train，但是就不要从现有 train 随机抽 27 张做新 val了
步骤 2：加一个轻量增强 — bbox_random_shrink，在数据加载时把每个 GT bbox 随机缩放到 0.8~1.2 倍吧，生成额外的伪 GT。另外，我还是希望实现实现 Coverage Loss，保留原有的Iou损失等等不变,添加覆盖度损失作为一个新的损失参考项，给予一定权重，这样即使不理想，也可以删除，不影响已有的源码
步骤 3（可选）：训练策略微调 — 不沿用现有 best，从预训练模型从头开始。新增 --multi-scale可以，但是得保持图片长宽比不变，无法保持就不加。
微调 hyp_aug.yaml：copy_paste: 0.2，降 bbox loss 权重，升分类 loss 权重，mosaic如果会改变训练图片长宽比就不加，不会改变才能加。
在每个训练结果的文件夹里，都要保存一份文件，详细说明本次训练的配置